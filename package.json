{
  "name": "web-client",
  "packageManager": "yarn@3.0.2",
  "dependencies": {
    "@babel/cli": "^7.15.7",
    "@babel/core": "^7.15.8",
    "@babel/preset-react": "^7.14.5"
  },
  "scripts": {
    "watch": "cp src/index.html out/index.html && npx babel --watch src --out-dir out --presets @babel/react"
  },
  "devDependencies": {
    "@types/babel__core": "^7"
  }
}
